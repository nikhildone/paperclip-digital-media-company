import { readFileSync, writeFileSync } from "node:fs";

const target = "ui/src/components/AgentConfigForm.tsx";
let source = readFileSync(target, "utf8");

if (!source.includes("SINK_DINK_MODEL_ROUTER_PROVIDER_OPTIONS")) {
  const constantsMarker = "const EMPTY_ENV: Record<string, EnvBinding> = {};\n";
  const constantsPatch = `${constantsMarker}\nconst SINK_DINK_MODEL_ROUTER_PROVIDER_OPTIONS = [\n  { value: \"\", label: \"Default / inherit\" },\n  { value: \"gemini\", label: \"Gemini\" },\n  { value: \"openrouter\", label: \"OpenRouter\" },\n  { value: \"groq\", label: \"Groq\" },\n  { value: \"deepseek\", label: \"DeepSeek\" },\n  { value: \"openai\", label: \"OpenAI\" },\n] as const;\n`;
  if (!source.includes(constantsMarker)) throw new Error("SINK DINK patch failed: constants marker not found");
  source = source.replace(constantsMarker, constantsPatch);
}

if (!source.includes("const sinkDinkModelRouter = !isCreate")) {
  const returnMarker = "  return (\n    <div className={cn(\"relative\", cards && \"space-y-6\")}>";
  const routerLogic = `  const sinkDinkModelRouter = !isCreate\n    ? (asObject(\n        eff(\n          \"adapterConfig\",\n          \"modelRouter\",\n          (asObject(config.modelRouter) ?? {}) as Record<string, unknown>,\n        ),\n      ) ?? {})\n    : {};\n\n  function updateSinkDinkModelRouter(field: string, value: unknown) {\n    if (isCreate) return;\n    const current = asObject(\n      eff(\n        \"adapterConfig\",\n        \"modelRouter\",\n        (asObject(config.modelRouter) ?? {}) as Record<string, unknown>,\n      ),\n    ) ?? {};\n    const next = { ...current };\n    const emptyString = typeof value === \"string\" && value.trim().length === 0;\n    const emptyNumber = typeof value === \"number\" && !Number.isFinite(value);\n    if (value === undefined || value === null || emptyString || emptyNumber) {\n      delete next[field];\n    } else {\n      next[field] = typeof value === \"string\" ? value.trim() : value;\n    }\n    mark(\"adapterConfig\", \"modelRouter\", next);\n  }\n\n  function clearSinkDinkModelRouter() {\n    if (!isCreate) mark(\"adapterConfig\", \"modelRouter\", undefined);\n  }\n\n${returnMarker}`;
  if (!source.includes(returnMarker)) throw new Error("SINK DINK patch failed: return marker not found");
  source = source.replace(returnMarker, routerLogic);
}

if (!source.includes("{/* ---- SINK DINK Model Router ---- */}")) {
  const runPolicyMarker = "\n\n      {/* ---- Run Policy ---- */}";
  const routerSection = `\n\n      {/* ---- SINK DINK Model Router ---- */}\n      {!isCreate && (\n        <div className={cn(!cards && \"border-b border-border\")}>\n          {cards\n            ? <h3 className=\"text-sm font-medium mb-3\">SINK DINK Model Router</h3>\n            : <div className=\"px-4 py-2 text-xs font-medium text-muted-foreground\">SINK DINK Model Router</div>\n          }\n          <div className={cn(cards ? \"border border-border rounded-lg p-4 space-y-3\" : \"px-4 pb-3 space-y-3\")}>\n            <p className=\"text-xs text-muted-foreground\">\n              Controls this agent only when SINK DINK production runs. Select which provider/API this agent should call. Values are saved into adapterConfig.modelRouter.\n            </p>\n            <div className=\"grid gap-3 sm:grid-cols-2\">\n              <Field label=\"Provider\" hint=\"Which model provider this SINK DINK agent should use.\">\n                <select\n                  className={inputClass}\n                  value={String(sinkDinkModelRouter.provider ?? \"\")}\n                  onChange={(event) => updateSinkDinkModelRouter(\"provider\", event.target.value || undefined)}\n                >\n                  {SINK_DINK_MODEL_ROUTER_PROVIDER_OPTIONS.map((option) => (\n                    <option key={option.value || \"default\"} value={option.value}>{option.label}</option>\n                  ))}\n                </select>\n              </Field>\n\n              <Field label=\"Model\" hint=\"Example: gemini-2.5-flash, google/gemini-2.5-flash-lite, llama-3.1-8b-instant.\">\n                <DraftInput\n                  value={String(sinkDinkModelRouter.model ?? sinkDinkModelRouter.modelId ?? \"\")}\n                  onCommit={(value) => updateSinkDinkModelRouter(\"model\", value || undefined)}\n                  immediate\n                  className={inputClass}\n                  placeholder=\"Provider default\"\n                />\n              </Field>\n\n              <Field label=\"Paperclip secret name\" hint=\"Secret name/key already saved in Paperclip Secrets. Do not paste the secret value here.\">\n                <DraftInput\n                  value={String(sinkDinkModelRouter.apiKeySecret ?? sinkDinkModelRouter.secretName ?? sinkDinkModelRouter.secret ?? \"\")}\n                  onCommit={(value) => updateSinkDinkModelRouter(\"apiKeySecret\", value || undefined)}\n                  immediate\n                  className={inputClass}\n                  placeholder=\"GEMINI_API_KEY / OPENROUTER_API_KEY / GROQ_API_KEY\"\n                />\n              </Field>\n\n              <Field label=\"Env key fallback\" hint=\"Optional HF/environment variable name fallback.\">\n                <DraftInput\n                  value={String(sinkDinkModelRouter.apiKeyEnv ?? \"\")}\n                  onCommit={(value) => updateSinkDinkModelRouter(\"apiKeyEnv\", value || undefined)}\n                  immediate\n                  className={inputClass}\n                  placeholder=\"Same as secret name usually\"\n                />\n              </Field>\n\n              <Field label=\"Temperature\" hint=\"Creativity. Common range: 0.2 to 1.0.\">\n                <DraftNumberInput\n                  value={Number(sinkDinkModelRouter.temperature ?? 0.7)}\n                  onCommit={(value) => updateSinkDinkModelRouter(\"temperature\", value)}\n                  immediate\n                  className={inputClass}\n                />\n              </Field>\n\n              <Field label=\"Max tokens\" hint=\"Output size limit for this agent.\">\n                <DraftNumberInput\n                  value={Number(sinkDinkModelRouter.maxTokens ?? 4096)}\n                  onCommit={(value) => updateSinkDinkModelRouter(\"maxTokens\", value)}\n                  immediate\n                  className={inputClass}\n                />\n              </Field>\n            </div>\n\n            <div className=\"flex items-center justify-between gap-3 rounded-md border border-border/70 px-3 py-2\">\n              <p className=\"text-xs text-muted-foreground\">\n                Leave fields blank to use system defaults. Save from the floating Save button after editing.\n              </p>\n              <Button type=\"button\" variant=\"outline\" size=\"sm\" onClick={clearSinkDinkModelRouter}>\n                Use defaults\n              </Button>\n            </div>\n          </div>\n        </div>\n      )}${runPolicyMarker}`;
  if (!source.includes(runPolicyMarker)) throw new Error("SINK DINK patch failed: run policy marker not found");
  source = source.replace(runPolicyMarker, routerSection);
}

writeFileSync(target, source);
console.log("SINK DINK Model Router UI patch applied.");
